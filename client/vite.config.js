/*
import { defineConfig } from "vite";

export default defineConfig({
  ssr: {
    noExternal: ["astro-cloudinary"], // Ajusta según tus necesidades
  },
  resolve: {
    alias: {
      // Puedes agregar alias para rutas más sencillas si es necesario
    },
  },
  build: {
    rollupOptions: {
      // Aquí puedes personalizar más opciones de build
    },
  },
});
*/
